import express from "express";

const router = express.Router();

router.post("/create-place", () => {});
router.get("/", () => {});
router.get("/:pid", () => {});
router.put("/:pid", () => {});
router.delete("/:pid", () => {});

module.exports = router;
